name: PR catalog database measurement

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: pr-catalog-database-measurement-${{ github.ref }}
  cancel-in-progress: true

jobs:
  measure:
    if: github.head_ref == 'perf/catalog-database-measurements'
    name: Catalog database measurement
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 180

    steps:
      - name: Checkout without pre-clean
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          clean: false

      - name: Clean workspace with Windows long paths
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          git config --local core.longpaths true
          git reset --hard HEAD
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to reset the catalog measurement workspace."
          }
          $cleanOutput = (& git clean -ffdx 2>&1 | Out-String).Trim()
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to clean the catalog measurement workspace.`n$cleanOutput"
          }
          $dirty = (& git status --porcelain=v1 2>&1 | Out-String).Trim()
          if ($LASTEXITCODE -ne 0 -or $dirty) {
            throw "Catalog measurement workspace is not clean."
          }

      - name: Validate harness syntax
        shell: pwsh
        run: |
          pwsh -NoProfile -File .\tools\android\Test-TvHarnessSyntax.ps1

      - name: Run current-TV catalog database measurement
        shell: pwsh
        run: |
          pwsh -NoProfile -File .\tools\android\Invoke-CatalogDatabaseDeviceValidation.ps1 `
            -SourceBranch "${{ github.head_ref }}" `
            -SourceCommit "${{ github.event.pull_request.head.sha }}" `
            -EvidenceRoot ".work/evidence/pr-catalog-database" `
            -NoDaemon

      - name: Upload catalog measurement evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: pr-catalog-database-measurement-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .work/evidence/pr-catalog-database/**/*.json
            .work/evidence/pr-catalog-database/**/*.log
            .work/evidence/pr-catalog-database/**/*.txt
            .work/evidence/pr-catalog-database/**/*.png
            **/build/reports/androidTests/**
            **/build/outputs/androidTest-results/**
          if-no-files-found: warn
          include-hidden-files: true
          retention-days: 7
